use result::Result;

fn safe_div(a: i32, b: i32) -> Result<i32, ()> {
    if b == 0 {
        Err(())
    } else {
        Ok(a / b)
    }
}

fn main() -> i32 {
    let r = safe_div(6, 3);
    if r.is_ok() && r.unwrap() == 2 {
        0
    } else {
        1
    }
}